# Install as /etc/nginx/conf.d/joebot-rate-limit.conf.
# Empty keys are not counted, so only login POST attempts consume the limit.
map $request_method $joebot_login_limit_key {
    default "";
    POST $binary_remote_addr;
}

limit_req_zone $joebot_login_limit_key zone=joebot_login:10m rate=5r/m;
